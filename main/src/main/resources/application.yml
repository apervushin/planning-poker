springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /api
postgresql:
  host: localhost:5432
  user: postgres
  password: postgres
  database: postgres
  schema: poker
  ssl: true
  sslFactory: org.postgresql.ssl.NonValidatingFactory
  sslMode: prefer
liquibase:
  change_log: classpath:liquibase/notifications-main-changelog-master.xml
