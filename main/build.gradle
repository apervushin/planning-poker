dependencies {
    implementation(project(":common"))
    implementation(project(":notifications"))
    implementation(project(":settings"))
}

bootJar {
    enabled = true
    mainClass = 'in.pervush.poker.Application'
}

jar {
    enabled = false
}